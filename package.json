{
  "name": "ts-starter",
  "version": "0.1.0",
  "author": "Henry Aguila",
  "files": [
    "build"
  ],
  "main": "build/main.js",
  "types": "build/main.d.ts",
  "type": "module",
  "scripts": {
    "ts": "tsx",
    "test": "vitest --dir=test run",
    "build": "shx rm -rf build && tsup src/main.ts -d=build --dts --format=esm"
  },
  "devDependencies": {
    "@types/node": "^20.12.3",
    "prettier": "^3.2.5",
    "shx": "^0.3.4",
    "tsup": "^8.0.2",
    "tsx": "^4.7.2",
    "typescript": "^5.4.3",
    "vitest": "^1.4.0"
  }
}
